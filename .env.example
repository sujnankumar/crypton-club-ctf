# MongoDB Atlas connection string
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.example.mongodb.net/ctf_db?retryWrites=true&w=majority

# NextAuth.js configuration
# Generate a secret with: openssl rand -base64 32
AUTH_SECRET=changeme_in_production_secret_key_12345
AUTH_URL=http://localhost:3000

# Vercel Blob (Optional - for file uploads)
BLOB_READ_WRITE_TOKEN=

# Admin Setup (for seed script)
ADMIN_EMAIL=admin@ctf.local
ADMIN_PASSWORD=admin123
